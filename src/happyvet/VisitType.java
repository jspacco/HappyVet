package happyvet;
/**
 * Use an enum here!
 * 
 * This is much better than using a String or an int
 * 
 * If we use a String, someone will eventually type it in wrong
 * 
 * If we use an int, then people can put in the wrong values.
 */
public enum VisitType
{
    REGULAR,
    EMERGENCY
}